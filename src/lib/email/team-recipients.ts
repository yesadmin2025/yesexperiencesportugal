/**
 * Internal YES team recipients — receive notifications for every contact form
 * submission and every completed booking. Keep in one file so we never miss an
 * address when a new lead source is wired up.
 */
export const TEAM_NOTIFICATION_RECIPIENTS = [
  "info@yesexperiencesportugal.com",
  "yesexperiences@gmail.com",
] as const;
